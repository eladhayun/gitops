apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - deployment.yaml
  - configmap.yaml
  # Note: tunnel-credentials-secret.yaml is NOT included
  # Create the secret manually with actual credentials

commonLabels:
  app.kubernetes.io/name: cloudflared
  app.kubernetes.io/component: tunnel

