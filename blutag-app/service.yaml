apiVersion: v1
kind: Service
metadata:
  name: blutag-app
  labels:
    app: blutag-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: blutag-app
