apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml

generators:
  - secrets-generator.yaml

namespace: blutag

commonLabels:
  app: blutag-pointer-push
