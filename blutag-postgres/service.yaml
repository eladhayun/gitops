apiVersion: v1
kind: Service
metadata:
  name: blutag-postgres
  labels:
    app: blutag-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: blutag-postgres
