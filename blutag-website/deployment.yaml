apiVersion: apps/v1
kind: Deployment
metadata:
  name: blutag-website
  labels:
    app: blutag-website
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blutag-website
  template:
    metadata:
      labels:
        app: blutag-website
    spec:
      containers:
      - name: blutag-website
        image: prodacr1234.azurecr.io/blutag-website:main-9f3cee2
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            cpu: 5m
            memory: 8Mi
          limits:
            cpu: 50m
            memory: 32Mi
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5

