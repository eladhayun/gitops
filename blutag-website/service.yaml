apiVersion: v1
kind: Service
metadata:
  name: blutag-website
  labels:
    app: blutag-website
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: blutag-website

