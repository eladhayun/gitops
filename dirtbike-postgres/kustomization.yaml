apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The legacy server and Service are intentionally pruned. Keep this application
# to manage the connection Secret consumed by dirtbike-specialist.
generators:
  - secrets-generator.yaml
