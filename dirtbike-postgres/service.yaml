apiVersion: v1
kind: Service
metadata:
  name: dirtbike-postgres
  labels:
    app: dirtbike-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: dirtbike-postgres
