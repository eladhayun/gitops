apiVersion: batch/v1
kind: Job
metadata:
  name: rag-shadow-xplor-20260818-v2
  labels:
    app: dirtbike-specialist
    purpose: rag-shadow-canary
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 2700
  template:
    metadata:
      labels:
        app: dirtbike-specialist-shadow-canary
        purpose: rag-shadow-canary
    spec:
      restartPolicy: Never
      containers:
        - name: reindex-shadow
          image: prodacr1234.azurecr.io/dirtbike-specialist:main-377111d
          imagePullPolicy: Always
          command:
            - ./server
            - reindex-shadow
            - f6c6ef1e9ff65b1f00637bd3abc7f2e2e7987517a13b986445f43f156d7bc3c7
          env:
            - name: APP_ENV
              value: "production"
            - name: ENABLE_RAG_SEMANTIC_V2_SHADOW
              value: "true"
            - name: AZURE_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: azure-openai-secrets
                  key: AZURE_OPENAI_API_KEY
            - name: AZURE_OPENAI_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: azure-openai-secrets
                  key: AZURE_OPENAI_ENDPOINT
            - name: AZURE_OPENAI_DEPLOYMENT_NAME
              value: "gpt-4o-mini"
            - name: AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME
              value: "text-embedding-3-large"
            - name: POSTGRES_HOST
              value: dirtbike-postgres
            - name: POSTGRES_DB
              value: dirtbike
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: dirtbike-postgres-secret
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dirtbike-postgres-secret
                  key: postgres-password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
