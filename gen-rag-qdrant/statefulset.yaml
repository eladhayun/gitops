apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gen-rag-qdrant
  labels:
    app: gen-rag-qdrant
spec:
  serviceName: gen-rag-qdrant
  replicas: 1
  selector:
    matchLabels:
      app: gen-rag-qdrant
  template:
    metadata:
      labels:
        app: gen-rag-qdrant
    spec:
      containers:
      - name: qdrant
        image: qdrant/qdrant:v1.12.4
        ports:
        - containerPort: 6333
          name: http
        - containerPort: 6334
          name: grpc
        env:
        - name: QDRANT__SERVICE__GRPC_PORT
          value: "6334"
        volumeMounts:
        - name: qdrant-data
          mountPath: /qdrant/storage
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 6333
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /readyz
            port: 6333
          initialDelaySeconds: 10
          periodSeconds: 15
  volumeClaimTemplates:
  - metadata:
      name: qdrant-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 2Gi
