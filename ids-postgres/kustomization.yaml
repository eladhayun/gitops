apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The legacy server and Service are intentionally pruned. Keep this application
# to manage the shared-PostgreSQL connection Secret consumed by IDS and its job.
generators:
  - secrets-generator.yaml
