FROM alpine:latest

# Install SSH client and netcat at build time
RUN apk add --no-cache openssh-client netcat-openbsd

# Create SSH directory
RUN mkdir -p /root/.ssh && chmod 700 /root/.ssh

# Default entrypoint - will be overridden by deployment command
ENTRYPOINT ["/bin/sh"]
