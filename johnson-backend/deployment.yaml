apiVersion: apps/v1
kind: Deployment
metadata:
  name: johnson-backend
  labels:
    app: johnson-backend
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: johnson-backend
  template:
    metadata:
      labels:
        app: johnson-backend
    spec:
      containers:
      - name: johnson-backend
        image: prodacr1234.azurecr.io/johnson-backend:1f0157ddf9a8707cdbe460f01e678238a6bfb92c
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: johnson-backend-config
        - secretRef:
            name: johnson-backend-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5

