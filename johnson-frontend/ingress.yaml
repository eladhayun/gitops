apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: johnson-frontend
spec:
  ingressClassName: nginx
  rules:
  - host: johnson.jshipster.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: johnson-frontend
            port:
              number: 80

