apiVersion: v1
kind: Service
metadata:
  name: johnson-frontend
  labels:
    app: johnson-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: johnson-frontend

