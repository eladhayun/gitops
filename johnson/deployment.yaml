apiVersion: apps/v1
kind: Deployment
metadata:
  name: johnson
  labels:
    app: johnson
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: johnson
  template:
    metadata:
      labels:
        app: johnson
    spec:
      initContainers:
      - name: johnson-migrations
        image: prodacr1234.azurecr.io/johnson-backend-migrations:main-3294223
        env:
        - name: JOHNSON_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: johnson-secrets
              key: JOHNSON_DATABASE_URL
      containers:
      - name: johnson
        image: prodacr1234.azurecr.io/johnson-backend:main-3294223
        ports:
        - containerPort: 8000
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        envFrom:
        - configMapRef:
            name: johnson-config
        - secretRef:
            name: johnson-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5

