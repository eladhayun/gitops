apiVersion: v1
kind: Service
metadata:
  name: jshipster-bastion
  labels:
    app: jshipster-bastion
spec:
  type: ClusterIP
  ports:
  - port: 22
    targetPort: 22
    protocol: TCP
    name: ssh
  selector:
    app: jshipster-bastion
