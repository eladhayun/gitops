apiVersion: v1
kind: ConfigMap
metadata:
  name: promtail-config
  namespace: loki
data:
  promtail.yaml: |
    server:
      http_listen_port: 3101
      grpc_listen_port: 9095
    
    positions:
      filename: /tmp/positions.yaml
    
    clients:
      - url: http://loki.loki.svc.cluster.local:3100/loki/api/v1/push
    
    scrape_configs:
      # Scrape all pod logs using file-based service discovery
      # This reads log files directly from /var/log/pods
      - job_name: kubernetes-pods
        file_sd_configs:
          - files:
              - /etc/promtail/file-sd/pod-logs.json
            refresh_interval: 10s
        
        pipeline_stages:
          # Extract metadata from file path: namespace_podname_uid/containername/restartcount.log
          - regex:
              expression: '^(?P<path>/var/log/pods/(?P<namespace>[^_]+)_(?P<pod_name>[^_]+)_(?P<pod_uid>[^/]+)/(?P<container_name>[^/]+)/(?P<restart_count>\d+)\.log)$'
              source: filename
          # Add labels from extracted metadata
          - labels:
              namespace: namespace
              pod: pod_name
              container: container_name
              node: __host__
          # Parse Docker JSON logs
          - docker: {}
          - output:
              source: message
