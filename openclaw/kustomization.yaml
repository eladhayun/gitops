apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

generators:
  - secrets-generator.yaml

namespace: openclaw

commonLabels:
  app: openclaw
