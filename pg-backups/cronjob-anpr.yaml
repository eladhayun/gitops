apiVersion: batch/v1
kind: CronJob
metadata:
  name: pg-backup-anpr
  labels:
    app: pg-backup
    database: anpr
spec:
  schedule: "30 3 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app: pg-backup
            database: anpr
        spec:
          restartPolicy: OnFailure
          initContainers:
          - name: pg-dump
            image: postgres:17-alpine
            command: ["/bin/sh", "-c"]
            args:
            - |
              set -eu
              TIMESTAMP=$(date +%Y-%m-%d)
              SQL_FILE="/backups/anpr-${TIMESTAMP}.sql"
              ARCHIVE_FILE="${SQL_FILE}.gz"
              echo "Backing up anpr..."
              pg_dump -h anpr-postgres.anpr.svc.cluster.local -p 5432 -U "${PGUSER}" -d anpr --no-password --file "${SQL_FILE}"
              test -s "${SQL_FILE}"
              gzip "${SQL_FILE}"
              gzip -t "${ARCHIVE_FILE}"
              test -s "${ARCHIVE_FILE}"
              echo "Backup validated: $(du -h "${ARCHIVE_FILE}" | cut -f1)"
            env:
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: ANPR_PGUSER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: ANPR_PGPASSWORD
            volumeMounts:
            - name: backup-data
              mountPath: /backups
            resources:
              requests:
                memory: "64Mi"
                cpu: "10m"
              limits:
                memory: "256Mi"
                cpu: "200m"
          containers:
          - name: upload
            image: mcr.microsoft.com/azure-cli:latest
            command: ["/bin/sh", "-c"]
            args:
            - |
              set -eu
              TIMESTAMP=$(date +%Y-%m-%d)
              FILENAME="anpr-${TIMESTAMP}.sql.gz"
              echo "Uploading to pg-backups/anpr/${TIMESTAMP}.sql.gz..."
              az storage blob upload \
                --account-name "${AZURE_STORAGE_ACCOUNT}" \
                --account-key "${AZURE_STORAGE_KEY}" \
                --container-name pg-backups \
                --name "anpr/${TIMESTAMP}.sql.gz" \
                --file "/backups/${FILENAME}" \
                --overwrite true
              echo "Upload complete."
            env:
            - name: AZURE_STORAGE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: AZURE_STORAGE_ACCOUNT
            - name: AZURE_STORAGE_KEY
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: AZURE_STORAGE_KEY
            volumeMounts:
            - name: backup-data
              mountPath: /backups
            resources:
              requests:
                memory: "32Mi"
                cpu: "10m"
              limits:
                memory: "512Mi"
                cpu: "100m"
          volumes:
          - name: backup-data
            emptyDir:
              sizeLimit: 1Gi
