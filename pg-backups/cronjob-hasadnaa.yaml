apiVersion: batch/v1
kind: CronJob
metadata:
  name: pg-backup-hasadnaa
  labels:
    app: pg-backup
    database: hasadnaa
spec:
  schedule: "30 2 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app: pg-backup
            database: hasadnaa
        spec:
          restartPolicy: OnFailure
          initContainers:
          - name: pg-dump
            image: postgres:16-alpine
            command: ["/bin/sh", "-c"]
            args:
            - |
              set -e
              TIMESTAMP=$(date +%Y-%m-%d)
              FILENAME="hasadnaa-${TIMESTAMP}.sql.gz"
              echo "Backing up hasadnaa..."
              pg_dump -h dirtbike-postgres.dirtbike.svc.cluster.local -p 5432 -U "${PGUSER}" -d hasadnaa --no-password | gzip > "/backups/${FILENAME}"
              echo "Done: $(du -h /backups/${FILENAME} | cut -f1)"
            env:
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: HASADNAA_PGUSER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: HASADNAA_PGPASSWORD
            volumeMounts:
            - name: backup-data
              mountPath: /backups
            resources:
              requests:
                memory: "64Mi"
                cpu: "10m"
              limits:
                memory: "256Mi"
                cpu: "200m"
          containers:
          - name: upload
            image: mcr.microsoft.com/azure-cli:latest
            command: ["/bin/sh", "-c"]
            args:
            - |
              set -e
              TIMESTAMP=$(date +%Y-%m-%d)
              FILENAME="hasadnaa-${TIMESTAMP}.sql.gz"
              echo "Uploading to pg-backups/hasadnaa/${TIMESTAMP}.sql.gz..."
              az storage blob upload \
                --account-name "${AZURE_STORAGE_ACCOUNT}" \
                --account-key "${AZURE_STORAGE_KEY}" \
                --container-name pg-backups \
                --name "hasadnaa/${TIMESTAMP}.sql.gz" \
                --file "/backups/${FILENAME}" \
                --overwrite true
              echo "Upload complete."
            env:
            - name: AZURE_STORAGE_ACCOUNT
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: AZURE_STORAGE_ACCOUNT
            - name: AZURE_STORAGE_KEY
              valueFrom:
                secretKeyRef:
                  name: pg-backup-secrets
                  key: AZURE_STORAGE_KEY
            volumeMounts:
            - name: backup-data
              mountPath: /backups
            resources:
              requests:
                memory: "32Mi"
                cpu: "10m"
              limits:
                memory: "512Mi"
                cpu: "100m"
          volumes:
          - name: backup-data
            emptyDir:
              sizeLimit: 1Gi
