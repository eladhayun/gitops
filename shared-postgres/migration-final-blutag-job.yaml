apiVersion: batch/v1
kind: Job
metadata:
  name: shared-postgres-final-blutag-20260901t064502z
  namespace: postgres
  annotations:
    argocd.argoproj.io/sync-wave: "73"
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 7200
  template:
    metadata:
      labels: {app: shared-postgres-migration, database: blutag, phase: final}
    spec:
      restartPolicy: Never
      initContainers:
        - name: dump
          image: postgres:17-alpine
          command: [/scripts/dump.sh]
          env:
            - {name: DATABASE, value: blutag}
            - {name: SOURCE_HOST, value: blutag-postgres.blutag.svc.cluster.local}
            - name: SOURCE_USER
              valueFrom: {secretKeyRef: {name: shared-postgres-migration-secrets, key: BLUTAG_PGUSER}}
            - name: SOURCE_PASSWORD
              valueFrom: {secretKeyRef: {name: shared-postgres-migration-secrets, key: BLUTAG_PGPASSWORD}}
          resources:
            requests: {cpu: 50m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
          volumeMounts:
            - {name: scripts, mountPath: /scripts, readOnly: true}
            - {name: migration, mountPath: /migration}
        - name: upload
          image: mcr.microsoft.com/azure-cli:latest
          command: [/scripts/upload.sh]
          env:
            - {name: DATABASE, value: blutag}
            - {name: MIGRATION_PREFIX, value: consolidation/final/20260901T064502Z}
            - name: AZURE_STORAGE_ACCOUNT
              valueFrom: {secretKeyRef: {name: shared-postgres-migration-secrets, key: AZURE_STORAGE_ACCOUNT}}
            - name: AZURE_STORAGE_KEY
              valueFrom: {secretKeyRef: {name: shared-postgres-migration-secrets, key: AZURE_STORAGE_KEY}}
          resources:
            requests: {cpu: 20m, memory: 64Mi}
            limits: {cpu: 200m, memory: 512Mi}
          volumeMounts:
            - {name: scripts, mountPath: /scripts, readOnly: true}
            - {name: migration, mountPath: /migration}
      containers:
        - name: restore
          image: pgvector/pgvector:pg17
          command: [/scripts/final-restore.sh]
          env:
            - {name: DATABASE, value: blutag}
            - {name: TARGET_USER, value: blutag}
            - {name: VECTOR_REQUIRED, value: "false"}
            - name: TARGET_ADMIN_PASSWORD
              valueFrom: {secretKeyRef: {name: shared-postgres-secrets, key: POSTGRES_ADMIN_PASSWORD}}
            - name: TARGET_PASSWORD
              valueFrom: {secretKeyRef: {name: shared-postgres-secrets, key: BLUTAG_PASSWORD}}
          resources:
            requests: {cpu: 100m, memory: 256Mi}
            limits: {cpu: "1", memory: 1Gi}
          volumeMounts:
            - {name: scripts, mountPath: /scripts, readOnly: true}
            - {name: migration, mountPath: /migration}
      volumes:
        - name: scripts
          configMap: {name: shared-postgres-migration, defaultMode: 0555}
        - name: migration
          emptyDir: {sizeLimit: 2Gi}
