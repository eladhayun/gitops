apiVersion: v1
kind: ServiceAccount
metadata:
  name: azure-billing-reporter
  labels:
    app: azure-billing-report
  annotations:
    azure.workload.identity/client-id: "${AZURE_CLIENT_ID}"
    azure.workload.identity/tenant-id: "${AZURE_TENANT_ID}"
