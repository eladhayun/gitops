apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The legacy server and Service are intentionally pruned. Keep this application
# to retain its SOPS-managed database credential material for rollback.
generators:
  - secrets-generator.yaml
