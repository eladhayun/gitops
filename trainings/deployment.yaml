apiVersion: apps/v1
kind: Deployment
metadata:
  name: trainings
  labels:
    app: trainings
spec:
  # Temporarily paused while the authoritative database is copied to the
  # shared PostgreSQL server. The cutover commit restores this to one.
  replicas: 0
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: trainings
  template:
    metadata:
      labels:
        app: trainings
    spec:
      containers:
      - name: trainings
        image: prodacr1234.azurecr.io/trainings:main-809d545
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: trainings-config
        - secretRef:
            name: trainings-secrets
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/healthz
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /api/healthz
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 15
